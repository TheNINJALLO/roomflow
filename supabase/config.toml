project_id = "bjqvowghqajwudgyqnau"

[functions.intake-lead]
verify_jwt = false

[functions.process-followups]
verify_jwt = false

[functions.townsquare-sync]
# User requests are still verified explicitly with auth.getUser(). This must be
# false so a paired Sync Station can use its own hashed device credential.
verify_jwt = false
